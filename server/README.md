# PAY.JP Checkout V2 サンプルサーバー

サーバーのセットアップ、環境変数、Webhook、クライアント起動手順はルートの [README.md](../README.md) に集約しています。

このディレクトリで通常使うコマンドは次の 2 つです。

```bash
npm install
npm start
```

サーバーは既定で `http://localhost:3000` で起動します。
